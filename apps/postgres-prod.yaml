apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgres-prod
  namespace: argocd
spec:
  project: petclinic-project
  source:
    repoURL: https://github.com/romangulyako/petclinic-infra.git
    targetRevision: main
    path: charts/postgres
    helm:
      valueFiles:
        - ../../environments/prod/postgres-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: petclinic-infra-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - PruneLast=true
